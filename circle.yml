machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - cd ./src/tools
    - pwd
    - docker build --rm=false ./src/tools/docker/base

#deployment:
#  hub:
#    branch: master
#    commands:
#      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#      - docker push circleci/elasticsearch
